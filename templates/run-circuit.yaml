spec:

  templates:

  - name: run-circuit

    parent: generic-task

    inputs:

      parameters:

      - name: command
        value: python3 -m site && python3 main.py

      artifacts:
      - name: main-script
        path: /app/main.py
        raw:
          data: |
            from pl_qe import simple_circuit
            simple_circuit()

    outputs:
      artifacts:
      - name: about
        path: /app/about.json
